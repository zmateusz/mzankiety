<%= simple_form_for(@survey) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :descr %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
